<template>
 <slot />
</template>

<script lang="ts" setup>
import type { Ref } from 'vue';
import { useSeoMeta, useSiteConfig, useRoute, ref, useRouter, useHead } from '#imports';

const props = defineProps({
 type: {
  type: String,
  default: 'Person',
 },
 name: String,
 url: String,
});

const route = useRoute();
const router = useRouter();

// if (!route.meta.linkedData) {
//  // 一時的に route.meta に値をセット
//  router.currentRoute.value.meta.linkedData = ref([]);
// }

// const linkedData = route.meta.linkedData as Ref<Array<unknown>>;
// console.log(linkedData.value);
// linkedData.value.push(
//  {
//   '@context': 'https://schema.org',
//   '@type': props.type,
//   'name': props.name || route.meta.title,
//  },
// );
</script>
