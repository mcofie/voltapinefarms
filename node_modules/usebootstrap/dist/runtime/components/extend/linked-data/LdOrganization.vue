<template>
 <slot />
</template>

<script lang="ts" setup>
import type { Ref } from 'vue';
import { inject } from '#imports';

const props = defineProps({
 type: {
  type: String,
  default: 'Organization',
 },
 name: String,
 url: String,
});

const ldOrganization = inject<Ref<[]>>('ld-organization');
if (ldOrganization) {
 ldOrganization.value.push(props);
}
</script>
