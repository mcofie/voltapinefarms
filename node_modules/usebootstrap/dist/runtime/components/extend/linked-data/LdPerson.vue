<template>
 <slot />
</template>

<script lang="ts" setup>
import type { Ref } from 'vue';
import { inject } from '#imports';

const props = defineProps({
 type: {
  type: String,
  default: 'Person',
 },
 name: String,
 url: String,
});

const ldPerson = inject<Ref<[]>>('ld-person');
if (ldPerson) {
 ldPerson.value.push(props);
}
</script>
